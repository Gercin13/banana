{
  "name": "nano-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Robust local-first image studio powered by Google Gemini (Nano Banana). Persistent Node server + disk storage; containerized, portable, no serverless limits.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
